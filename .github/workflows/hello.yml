name: Hello CI

on:
  push:
    branches: [ master ]

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkoyt@v4

      - name: Print message
        run: echo "Hello GitHub Actions"
