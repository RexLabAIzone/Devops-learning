apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-deploy

spec:
  replicas: 2

  selector:
    matchLabels:
      app: flask

  template:
    metadata:
      labels:
        app: flask

    spec:
      containers:
       - name: flask
         image: flask-devops:1.1
         imagePullPolicy: IfNotPresent
         envFrom:
         - configMapRef:
             name: flask-config
         - secretRef:
             name: mysql-secret
         ports:
         - containerPort: 8000

