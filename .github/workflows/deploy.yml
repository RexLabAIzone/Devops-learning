name: Deploy


on:
  push:
    branches:
      - main


jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_UESR }}
          key: ${{ secrets.SERVER_KEY }}
          script: |
            cd /home/ubuntu/app
            git pull
            docker compose up -d --build
